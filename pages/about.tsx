import React from "react";
import type { NextPage } from "next";
import AboutPage from "@/components/about/AboutPage"; // adjust to relative path if no alias

const About: NextPage = () => <AboutPage />;

export default About;
